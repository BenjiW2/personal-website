# Personal Website

Single-page static personal website.

## Structure

- `index.html` contains the full site
- `styles/site.css` contains the visual system and layout
- `assets/resume.pdf` is the resume target for the top-right button
